@model List<Company>
<partial name="_Notificatios" />
<div class=" card shadow border-0 my-4 ">

    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white p-0">
                    Create Company
                </h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <div class="container">
            <div class="row pt-4 pb-3">
                <div class="col-6">
                    <h2 clast="text-primary"></h2>

                </div>
                <div class="col-6 text-end">
                    <a asp-controller="Company" asp-action="Upsert" class="btn btn-primary">
                        CREATE NEW LIST<i class="bi bi-plus-circle"></i>
                    </a>
                </div>
            </div>
        </div>

        <table id="tblData" class="table table-bordered table-striped">
            <thead>
                <tr>

                    <th>
                        Name
                    </th>
                    <th>
                        StreetAddress
                    </th>
                    <th>
                        State
                    </th>

                    <th>
                        City
                    </th>


                    <th>PhoneNumber</th>
                    <th>PostalCode</th>

                </tr>
            </thead>

            @*<tbody>
                    @foreach (var obj in Model.OrderBy(u => u.id))
                    {
                        <tr>
                            <td>@obj.Author</td>
                            <td>@obj.Title</td>

                            <td>@obj.ISBN</td>
                            <td>@obj.Description</td>

                            <td>@obj.Price</td>

                            <td>@obj.Catogory.Name</td>

                            <td>
                                <div class="w-75 btn-group" role="group">
                                    <a asp-controller="Product" asp-action="Upsert" asp-route-Id="@obj.id" class="btn btn-primary mx-2"><i class="bi bi-pencil-square"></i>Edit</a>
                                    <a asp-controller="Product" asp-action="Delete" asp-route-Id="@obj.id" class="btn btn-danger mx-2"><i class="bi bi-trash"></i>Delete</a>

                                </div>
                            </td>
                        </tr>



                    }
                </tbody>*@
        </table>
    </div>
</div>

@section Scripts{
    <script src="~/js/company.js"></script>


}
    